package tunes;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class Question6Tests {
  /*
  @Test
  public void testGetTotalDuration1() {
    Tune tune1 = new StandardTune();
    Tune tune2 = new TransposedTune(tune1, 10);
    assertEquals(0, tune1.getTotalDuration());
    assertEquals(0, tune2.getTotalDuration());
  }

  @Test
  public void testGetTotalDuration2() {
    Tune tune1 = new StandardTune();
    tune1.addNote(new Note(10, 10));
    tune1.addNote(new Note(10, 2));
    tune1.addNote(new Note(10, 7));
    tune1.addNote(new Note(10, 15));
    Tune tune2 = new TransposedTune(tune1, 10);
    assertEquals(34, tune1.getTotalDuration());
    assertEquals(34, tune2.getTotalDuration());
  }

  @Test
  public void testGetTotalDuration3() {
    Tune tune1 = new StandardTune();
    for (int i = 1; i < 30; i++) {
      tune1.addNote(new Note(0, i));
    }
    Tune tune2 = new TransposedTune(tune1, 10);
    assertEquals(435, tune1.getTotalDuration());
    assertEquals(435, tune2.getTotalDuration());
  }
  */
}
