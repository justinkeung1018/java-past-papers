import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.Test;

public class NineTailsWeightedGraphTest {

  NineTailsWeightedGraph graph;

  // UNCOMMENT THE TEST BELOW ONCE YOU HAVE IMPLEMENTED constructGraph() in NineTailsWeightedGraph
  /*
  @Before
  public void initializeGraph() {
    graph = new NineTailsWeightedGraph();
  }
  */

  // UNCOMMENT THE TESTS BELOW ONCE YOU HAVE IMPLEMENTED constructMinimumSpanningTree()
  //      in NineTailsWeightedGraph
  /*
  @Test
  public void shortestPath1() {
    String config = "HHHHHHHHH";
    int configIdx = graph.configurationToIndex(config.toUpperCase().toCharArray());
    assertEquals(17, graph.getShortestPath(configIdx));
  }

  @Test
  public void shortestPath17() {
    String config = "HHHHTHHHH";
    int configIdx = graph.configurationToIndex(config.toUpperCase().toCharArray());
    assertEquals(28, graph.getShortestPath(configIdx));
  }

  @Test
  public void shortestPath115() {
    String config = "HHTTTHHTH";
    int configIdx = graph.configurationToIndex(config.toUpperCase().toCharArray());
    assertEquals(9, graph.getShortestPath(configIdx));
  }
   */

}
