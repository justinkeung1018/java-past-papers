package tunes;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertSame;
import static org.junit.Assert.assertTrue;

import java.util.List;
import org.junit.Test;

public class Question4Tests {
  /*
  @Test
  public void testConstructor() {
    Tune tune = new StandardTune();
    assertTrue(tune.getNotes().isEmpty());
  }

  @Test
  public void testGetNotesReturnsCopy() {
    Tune tune = new StandardTune();
    assertTrue(tune.getNotes().isEmpty());
    final List<Note> notes = tune.getNotes();
    notes.add(new Note(1, 2));
    assertTrue(tune.getNotes().isEmpty());
  }

  @Test
  public void testAddNote() {
    Tune tune = new StandardTune();
    final Note note1 = new Note(1, 4);
    final Note note2 = new Note(2, 5);
    final Note note3 = new Note(3, 6);
    final Note note4 = new Note(4, 7);

    tune.addNote(note1);
    assertEquals(1, tune.getNotes().size());
    assertSame(note1, tune.getNotes().get(0));

    tune.addNote(note2);
    assertEquals(2, tune.getNotes().size());
    assertSame(note1, tune.getNotes().get(0));
    assertSame(note2, tune.getNotes().get(1));

    tune.addNote(note3);
    assertEquals(3, tune.getNotes().size());
    assertSame(note1, tune.getNotes().get(0));
    assertSame(note2, tune.getNotes().get(1));
    assertSame(note3, tune.getNotes().get(2));

    tune.addNote(note4);
    assertEquals(4, tune.getNotes().size());
    assertSame(note1, tune.getNotes().get(0));
    assertSame(note2, tune.getNotes().get(1));
    assertSame(note3, tune.getNotes().get(2));
    assertSame(note4, tune.getNotes().get(3));
  }
  */
}
