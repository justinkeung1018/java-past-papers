package tunes;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotEquals;
import static org.junit.Assert.assertTrue;

import java.util.HashSet;
import java.util.Set;
import org.junit.Test;

public class Question2Tests {
  /*
  @Test
  public void testEquals1() {
    final Note note = new Note(1, 3);
    assertEquals(note, note);
  }

  @Test
  public void testEquals2() {
    final Note note1 = new Note(1, 3);
    final Note note2 = new Note(1, 3);
    assertEquals(note1, note2);
  }

  @Test
  public void testEquals3() {
    final Note note1 = new Note(1, 3);
    final Note note2 = new Note(2, 3);
    assertNotEquals(note1, note2);
  }

  @Test
  public void testEquals4() {
    final Note note1 = new Note(1, 3);
    final Note note2 = new Note(1, 4);
    assertNotEquals(note1, note2);
  }

  @Test
  public void testEquals5() {
    final Note note = new Note(1, 3);
    assertNotEquals(note, null);
  }

  @Test
  public void testEquals6() {
    final Note note = new Note(1, 3);
    assertNotEquals(note, note.toString());
  }

  @Test
  public void testEquals7() {
    final Note note1 = new Note(1, 3);
    final Note note2 = new Note(1, 3);
    final Set<Note> notes = new HashSet<>();
    notes.add(note1);
    assertTrue(notes.contains(note2));
  }
  */
}
